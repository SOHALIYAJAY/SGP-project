Add this backend folder to the your frontend folder (SGP_main_project)

How to run backend >

( command in terminal )

cd backend

pip install -r requirements.txt

uvicorn main:app --reload --host 0.0.0.0 --port 8000


(END)

